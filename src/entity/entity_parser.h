#include <vector>
#include "entity.h"

///*
// Reads text and converts it to entities.
std::vector<BaseEntity *> parse_entities(const char *data);
// std::vector<EntityID> parse_and_add_entities(const char *data);

// TODO(ed): Add a way to serialize entities.
