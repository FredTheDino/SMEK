#include "../test.h"
#include "renderer.h"
#include "../util/util.h"

#if 0
TEST_CASE("rendering_init", {
    // TODO(ed): Defer macro.
    GFX::init(game, "blargh!", 100, 100);
    defer { GFX::deinit(game); };

    return false;
});
#endif
