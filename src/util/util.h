#pragma once
// Convinience header for including all of the important stuff in util
// you probably want.
#include "color.h"
#include "defer.h"
#include "log.h"

#define LEN(x) (sizeof(x) / sizeof(x[0]))

